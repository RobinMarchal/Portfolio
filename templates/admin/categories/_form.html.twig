{{ form_start(form) }}
<div class="row">
    <div class="col-md-7">
        {{ form_row(form.name) }}
        {{ form_row(form.slug) }}
    </div>
</div>
<hr>

    {{ form_widget(form) }}
    <button class="btn btn-fonce waves-effect waves-light">{{ button|default('Enregistrer')}}</button>
{{ form_end(form) }}